.page-header
  %h2= "Places"
%table.table.table-striped
  %thead
    %tr
      %th= "Name"
      %th= "Street"
      %th= "City"
      %th= "Country"
      %th= "Created_at"
      %th= "Actions"
  %tbody
    - places.each do |place|
      %tr
        %td= place.name
        %td= place.street
        %td= place.city
        %td= place.country
        %td= place.created_at
        %td
          = link_to "Show", place_path(place), class: 'btn'
          = link_to "Edit", edit_place_path(place) , class: 'btn btn-info'
          = link_to "Delete", place_path(place), method: :delete, data: { confirm: t('.confirm', default: t("helpers.links.confirm", default: 'Are you sure?')) }, class: 'btn btn-warning'

