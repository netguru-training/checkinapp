.page-header
  %h2 Users
%table.table.table-striped
  %thead
    %tr
      %th Name
      %th E-mail
      %th CheckIn
      %th Actions
  %tbody
    - users.each do |user|
      - if user != current_user
        %tr
          %td= user.full_name
          %td= user.email
          %td= user.last_checkin
          %td
            = link_to "Show", '#', class: 'btn'
            - if user.has_pending?(current_user)
              = link_to "Cancel invitation", friendships_path, method: :delete , class: 'btn btn-warning'
            - elsif user.has_friend?(current_user)
              = link_to "Remove from friend", friendships_path, method: :delete , class: 'btn btn-warning'
            - else
              = link_to "Invite", friendships_path(receiver_id: user.id), method: :post , class: 'btn btn-info'
