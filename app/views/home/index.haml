<<<<<<< HEAD
.starter-template
  %h1 Check In App
  %p.lead
    %table.table.table-striped
      %thead
        %tr
          %th Name
          %th E-mail
          %th Actions
      %tbody
        - if @friends_near != nil
          - @friends_near.each do |friend|
            %tr
              %td= friend.full_name
              %td= friend.email
              %td
                = link_to "Show", user_path(friend), class: 'btn btn-info'
                = link_to "Delete from friend", '', class: 'btn btn-warning'
