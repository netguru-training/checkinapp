.row
  .row.col-md-6.col-md-offset-2.custyle.form-horizontal
    %legend
      Edit #{resource_name.to_s.humanize}
    = simple_form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f|
      = f.error_notification
      .form-group
        = f.label :first_name, class: "control-label col-sm-4"
        .col-sm-6
          = f.text_field :first_name, required: true, autofocus: true, class: "form-control"
      .form-group
        = f.label :last_name, class: "control-label col-sm-4"
        .col-sm-6
          = f.text_field :last_name, required: true, autofocus: true, class: "form-control"
      .form-group
        = f.label :email, class: "control-label col-sm-4"
        .col-sm-6
          = f.text_field :email, required: true, autofocus: true, class: "form-control"
        - if devise_mapping.confirmable? && resource.pending_reconfirmation?
          %p
            Currently waiting confirmation for: #{resource.unconfirmed_email}
      .form-group
        = f.label :password, class: "control-label col-sm-4"
        .col-sm-6
          = f.password_field :password, autocomplete: "off", hint: "leave it blank if you don't want to change it", required: false, class: "form-control"
      .form-group
        = f.label :password_confirmation, required: false, class: "control-label col-sm-4"
        .col-sm-6
          = f.password_field :password_confirmation, required: false, class: "form-control"
      .form-group
        = f.label :current_password, required: true, class: "control-label col-sm-4"
        .col-sm-6
          = f.password_field :current_password, hint: "we need your current password to confirm your changes", required: true, class: "form-control"
      .form-group
        .col-sm-offset-2.col-sm-10
          .pull-right
            = link_to 'Back', :back, class: "btn btn-default"
            = f.submit 'Update', class: "btn btn-primary"
    %h3 Cancel my account
    %p
      Unhappy? #{link_to "Cancel my account", registration_path(resource_name), data: { confirm: "Are you sure?" }, method: :delete}